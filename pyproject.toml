[project]
name = "DSP"
version = "0.1.0"
description = "Learn DSP"
requires-python = ">=3.12"
dependencies = [
    "librosa >= 0.11.0",
    "numpy",
    "pytest",
    "soundfile",
    "sounddevice",
    "scipy"
]